Based on the original ideas, reference sources, method hints, experimental thoughts, and paper format provided in `Input.md`, please fully leverage your professional expertise and conduct research according to the following process.  

Step 1. Conduct a literature survey based on the original ideas in `Input.md`, thoroughly searching top conferences and journals in the relevant field, and the latest important papers on arXiv, as follows:  
(1) Identify relevant papers and programmatically verify their existence using Semantic Scholar, arXiv or Google Scholar. For each candidate paper, query the paper's title, check that the returned result matches the title, authors, and venue, and discard any paper that cannot be confirmed. Record the verification status (confirmed/discarded) and the source for each paper;  
(2) Rank these papers by relevance and authority using your scientific judgment, then select up to 10 papers for careful study. For the top 5 ranked papers, perform more in-depth thinking and analysis, and produce an analysis document `Related_work.md`. If open-source code is found for the paper, carefully read the open-source code and document the specific methods and contributions. List an extended bibliography containing 20+ additional papers for use in subsequent academic writing.  

Step 2. Use your expertise in the relevant field to evaluate the originality and feasibility of the original idea, as follows:  
(1) With reference to the survey results in `Related_work.md`, think about what problem the original idea aims to solve, why it is important, and what the essential limitations of existing methods are, and formally define the problem.  
(2) Carefully think through and organize your ideas based on `Input.md`. You may iterate to achieve good originality and feasibility, then produce the refined idea document `Idea.md`. If originality or feasibility is insufficient, the orchestrator will return to "Step 1." to refine and improve the original idea, and conduct the survey again.  

Step 3. Based on the survey results and the key surveyed papers, further refine the idea and formulate a detailed solution method, as follows:  
(1) Using your expertise in the relevant field and referring to `Related_work.md`, evaluate the feasibility and experimental difficulty of the proposed solution. If feasibility is low or experimental difficulty is too high, try improving the method to achieve higher feasibility, then produce the solution-method document `Methodology.md` and update `Idea.md` accordingly. Ensure that the statements (especially mathematical proofs) in `Methodology.md` and `Idea.md` are rigorous and accurate, and that the sub-methods have systematic connections with one another rather than being merely a pile of methods;  
(2) Design a minimal proof-of-concept experiment that tests all the innovations of the proposed method in isolation. Create a subdirectory "`exp/initial_check`" and put the experimental code inside it. The goal is to verify that the core technical idea works in principle. Do NOT implement baselines or run comparisons at this stage. If these small experiments indicate insufficient effectiveness, or if any proposed core component cannot be validated, or if the method is not novel enough or differs too little from the references, return to "Step 3.(1)", adjust the method again in `Idea.md` and `Methodology.md`, and document the reasons in `Initial_check.md`, with up to 5 rounds of adjustment, then produce the small-scale experiment document `Initial_check.md `;  
(3) Programmatically detect available hardware resources and write the **hardware information** to `Experiment.md`. Detection should cover, at a minimum, GPU, CPU, RAM, Disk, to ensure subsequent experiments can fully utilize hardware resources for improved efficiency;  
(4) Use your expertise in the relevant field to determine evaluation methods, baselines, and metrics based on: (a) community adoption; (b) scale appropriateness; (c) fair comparison. Referring to `Input.md` and `Methodology.md`, formulate the **experimental plan** in `Experiment.md`, including baseline comparisons, ablation studies, etc., and plan the experimental procedure in detail to verify the method's effectiveness and address the main objective (Note: for purely theoretical research, you may design fewer experiments, or skip experiment design and directly move to "Step 6.");  
(5) Perform a resource-readiness check before proceeding: verify that all required model weights, datasets, software dependencies, and computational resources are available and accessible. For each model listed in the experimental plan, confirm that the weights can be loaded locally or downloaded successfully; for each dataset, confirm it is accessible and in the expected format. If any resource is missing or unavailable, attempt to resolve the issue (e.g., retry downloads, switch to alternative mirrors, install missing packages). If a critical resource remains unavailable after reasonable attempts, revise the experimental plan in `Experiment.md` accordingly and document the change with a clear justification, then assess whether the revised plan still adequately addresses the objectives in `Input.md`;  

Step 4. Following the refined idea, the experimental plan in `Experiment.md`, and with reference to the survey results, key surveyed papers, and small-experiment results, implement the solution method and complete baseline experiments on a basic dataset to verify correctness, as follows:  
(1) Follow `Experiment.md` to implement the solution method step by step, create a subdirectory "`exp/normal_exp`" and put the experimental code inside it, and progressively check results from key modules to ensure code correctness. The experimental code should include: (a) a `README.md` with clear instructions on how to run the experiments; (b) a `requirements.txt` or `environment.yaml` listing all dependencies; (c) a configuration file (e.g., `config.yaml`) for all hyperparameters, avoiding hard-coded values; (d) modular code structure separating data loading, model definition, training logic, and evaluation;  
(2) Implement the complete pipeline and verify end-to-end correctness on a basic setting. Run the experiment under reasonable configuration according to the **hardware information** in `Experiment.md`. Include ONE representative baseline for sanity-checking, but do not pursue performance optimization or comprehensive comparison. The goal is to ensure the full codebase runs correctly from data loading to metric computation. Produce an updated `Experiment.md` with **preliminary experiment results**;  
(3) After verifying correctness, produce a code-block documentation file `Codebase_guide.md` that records: (a) a list of all method-critical source files with their paths; (b) for each file, the key code blocks (with line ranges or function/class names) that are directly related to the proposed method; (c) brief descriptions of what each code block does and how it connects to the method described in `Methodology.md`. The goal is that a reader who only reads `Experiment.md` and `Codebase_guide.md` can understand which code to read and modify to reproduce or extend the experiments, without needing to explore the entire codebase.  

Step 5. Based on `Experiment.md` and `Codebase_guide.md`, complete the full experiments and ablation studies by reading and modifying only the critical code blocks, as follows:  
(1) Read `Experiment.md` to understand the hardware information, the experimental plan and the current baseline results, and read `Codebase_guide.md` to locate the critical code files and code blocks. You may need to make rough estimates of the time and hardware resources required for each group of experiments, and then parallelize these experiments as reasonably as possible to reduce overall duration.  
(2) Following the experimental plan in `Experiment.md`, modify the critical code blocks identified in `Codebase_guide.md` to run the complete experiments (e.g., on larger datasets, with different configurations, or with additional baselines), update `Experiment.md` with **comprehensive experiment results**. Do NOT explore or modify code outside the scope documented in `Codebase_guide.md` unless absolutely necessary; if new files must be touched, update `Codebase_guide.md` accordingly;  
(3) Conduct ablation studies to verify the contribution of each sub-method. For each ablation, disable or replace one component at a time, run the experiment, and compare results against the full method. Allow up to 5 rounds of adjustment to thoroughly test the rationality of the method. For each adjustment, document the motivation (why this change), the changes made, and the results comparison in a structured log file or experiment tracking tool (e.g., wandb or a simple CSV/JSON file); **Ablation study results** should also be documented in `Experiment.md`;  
(4) If, after sufficient optimization and adjustment, the experimental results still cannot demonstrate the effectiveness of the proposed solution method, the orchestrator will return to "Step 3." and optimize the method. Finally, produce the updated `Experiment.md` containing all results and other necessary information, and produce the conclusion document `Conclusion.md`;  
(5) Cross-check the experimental results against all core objectives listed in `Input.md`. For each objective, note whether it has been addressed by the experiments. Any unaddressed objective must be explained in the Limitations section of `Conclusion.md`, with an assessment of whether supplementary experiments are needed.  

Step 6. Based on `Related_work.md`, `Idea.md`, `Methodology.md`, and `Experiment.md`, write the paper using the provided template, as follows:  
(1) First, organize `Related_work.md`, `Idea.md`, `Methodology.md`, `Experiment.md`, and `Conclusion.md`, and write the paper outline `Outline.md`, referencing the structure of articles in the related work and original information from `Input.md`. The outline should include at least **Abstract**, **Introduction**, **Related Work**, **Methodology**, **Experiments** (purely theoretical papers may omit the Experiments section), and **Conclusion**. You may adjust section titles according to your professional judgment, decide whether an appendix or supplementary materials need to be included, and  plan the paper's core arguments and narrative logic;  
(2) Second, organize and manage all references systematically. Collect all papers cited in `Related_work.md` and other relevant literature, create a BibTeX file `references.bib` with complete and accurate bibliographic information for each reference, ensure citation keys follow a consistent naming convention, and verify that all references are properly formatted according to the target venue's requirements;  
(3) Next, design the figures needed based on the above materials. For figures that illustrate the method (if necessary), draw each figure on a separate PPT slide. After completing each figure, review it at least 5 times to ensure visual quality and accuracy, then crop extra whitespace and save it as a PDF named `figure_1.pdf`, `figure_2.pdf`, and so on. For necessary figures that present experimental results, use Python for plotting. After completing each figure, review it at least 5 times, adjust formatting, ensure visual quality and accuracy, then save it as a PDF, continuing the numbering from the method figures (e.g., `figure_n.pdf`, and so on);  
(4) Then, based on `Related_work.md`, `Idea.md`, `Methodology.md`, `Initial_check.md`, `Experiment.md`, `Conclusion.md`, and the pre-drawn `figure_1.pdf`, `figure_2.pdf`, etc., write the paper chapter by chapter according to `Outline.md` and the provided paper template;  
(5) Repeatedly check and polish the paper no fewer than 5 times to ensure overall consistency, coherence between sections, and high-quality formatting, and to ensure compliance with formatting requirements. Produce `paper.tex` and compile it into `paper.pdf`. If LaTeX compilation fails, read the compilation log, identify the error (e.g., missing packages, undefined references, float placement issues, encoding problems), fix it in `paper.tex`, and retry compilation, with up to 10 attempts;  
(6) Focus on assessing the similarity between the completed paper and existing literature, especially the references in `Related_work.md`, ensuring that the paper's statements, theoretical derivations, etc., are sufficiently different from existing papers. Then highlight the paper's novelty in the **Abstract** and **Introduction** while ensuring precise phrasing, and finally repeatedly check and polish to ensure coherence and consistency throughout, producing the final versions `paper.tex` and `paper.pdf`.  
